general {
    disable_loding_bar = true
}

background {
    monitor =
    path = /home/yoru/Pictures/Backgrounds/animegirldarkhair.png
}

input-field {
    monitor =
    size = 200, 50
    outline_thickness = 3
    dots_size = 0.2 # Scale of input-field height, 0.2 - 0.8
    dots_spacing = 0.5 # Scale of dots' absolute size, 0.0 - 1.0
    dots_center = false
    outer_color = rgba(f7768ebb)
    inner_color = rgba(240, 240, 240, 0.7)
    font_color = rgb(10, 10, 10)
    fade_on_empty = true
    placeholder_text = <i>Input Password...</i> # Text rendered in the input box when it's empty.
    hide_input = false

    position = 0, -20
    halign = center
    valign = center
}
